// Kept as a local compatibility import for editor modules. The implementation
// lives in the shared React package so video/export dialogs use the same modal contract.
export { useModalDialog, type ModalDialogOptions } from '@bendyline/squisq-react';
