/**
 * Review: the generalization of proofing to any provider that can find
 * something worth saying about a document.
 *
 * Pure and dependency-free, like `../proof`. The provider contract the editor
 * drives lives in `@bendyline/squisq-editor-react/review`.
 */

export * from './types.js';
